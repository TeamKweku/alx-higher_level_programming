#!/usr/bin/python3
def number_keys(a_dictionary):
    """ function that returns the number of keys
        in a dictionary

        Args:
            a_dictionary: dict provided

        Returns:
            number of keys in a_dictionary
    """
    return len(a_dictionary.keys())
